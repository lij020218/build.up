# 라이브 앱 QA 원장 + 다음 세션 핸드오프 (13~18단계) — 2026-06-26

사장님 전수 테스트 결과. **이 문서 하나로 다음 세션이 재질문 없이 바로 진행**할 수 있게 정리.

---

## 0. 확정 사항 (재질문 금지 — 이미 결정됨)
1. **진행 방식**: 기능 버그(A) 먼저 → 외부 URL(B). (사장님 선택 완료)
2. **URL 원칙(1-참고)**: 만료/이동되는 **개별 사업 상세페이지 딥링크 금지** → **주관기관 안정 메인/사업목록 페이지**로 전환. (cf. 메모리 `reference_hometax_url` — 홈택스는 PC 메인 직접 URL)
3. **만료 프로그램**: 일정 지난 건(예: 모두의 창업 프로젝트 2026)은 **삭제** 또는 `isExpired()` 필터로 자동 숨김.
4. **각 URL은 WebSearch로 현행 검증 후 수정**(글로벌 규칙). 추측 금지.
5. **web=iOS 동일 원칙**: SSOT 콘텐츠(hiring-setup 등) URL 수정 시 `npx tsx scripts/gen-stage-content-json.mts` 재실행해 iOS도 반영.

상태: ✅수정완료 / 🔎조사필요 / ⬜URL교정대기

---

## A. 기능 버그

| # | 증상 | 상태 | 비고 |
|---|---|---|---|
| A1a | 대출가이드 [내 업종 맞춤]이 업종 무관 전체 노출(미용실에 AI스타트업·로보틱스·농업) | ✅ `9c6f233` | `getMatchedPrograms` 결과를 `industries` 기준 업종 필터(LoanGuideStage.tsx:217). |
| A1b | [파운드원 1차 창업지원금] 클릭 → 화면 안 뜨고 루프 | ✅ `9c6f233` | internalApply(url="")가 `<a href="">`로 렌더돼 재로드 → 비링크 카드 + 안내(LoanGuideStage.tsx:533). |
| A3 | 14단계 메뉴 라인업, 미용·소매에 SaaS 예시(「Pro·AI 무제한」) 뜸 | ✅ `f397a2c` | SimplePlaceholderPanel에 example prop 분기. iOS는 이미 정상. |
| A4 | 15단계 공급처 같은 곳(hairnmi.co.kr) 여러 이름으로 중복 | ✅ `4062041` | url(없으면 name) 기준 dedup — 웹 getVendorData + iOS bundle. |
| **A2** | 대출가이드 [AI 사업계획서] 생성 시 **[7. 자금 용도 및 상환 계획]만** 뜸(전문 X) | ✅ `aade489` | 라우트만 response_format 누락 → gpt-5.4-mini 산문+말미 부분JSON, cleanup regex가 섹션7만 keep. json_object 강제 + max_completion_tokens 8192→12288. |

### A2 정밀 진단 (다음 세션 착수점)
- **렌더**: `LoanGuideStage.tsx:63` `bpSections` 상태(`Array<{title, content}>`). `generatePlan()` 257~304줄에서 **`/api/ai/business-plan/generate`** 호출(287줄) → **298줄 `setBpSections(data.sections)`**(API가 준 sections 배열을 그대로 신뢰, 클라이언트 분할 regex 없음). 아코디언 685줄 `bpSections.map`, `bpExpandedIdx`만 펼침.
- **핵심 가설**: "[7]만 뜸" = `bpSections.length === 1`(섹션7만 들어옴) 일 확률 높음 → **API 라우트가 1개 섹션만 반환**하거나 응답이 truncate.
- **다음 단계(딱 이것부터)**:
  1. 라우트 파일 열기 — `apps/web/app/api/ai/business-plan/generate/route.ts`(탐색 에이전트가 경로 미확정 — `find apps/web/app/api -path '*business-plan*'`로 확인). 응답이 `{sections: [{title,content}...]}` 전체인지, LLM 프롬프트/파싱이 마지막 섹션만 남기는지 확인.
  2. LLM 응답을 섹션별로 파싱하는 곳(라우트 또는 `@foundone/ai`)에서 **정규식이 마지막 매치만 keep**하는지, max_tokens로 잘리는지 점검.
  3. 수정 후 실제 생성 1회 돌려 7개 섹션 모두 오는지 확인.

---

## ✅ B 진행 완료 (2026-06-26)
- **B1 펀딩** `5ea12a3` — 13개 URL 교정 + 네이버 AI 해커톤 삭제(만료) + `isProgramExpired` 헬퍼·필터. ⚠️ 모두의 창업은 WebSearch 검증결과 2차 7월 활성 → 삭제 가정 뒤집고 유지. iOS codegen(122→121).
- **B2 공식사이트** `598b404` — 홈택스 websquare 슬래시 **인코딩(%2F)** 전수(SSOT 5곳 + 웹 7곳 + iOS BUHometaxLink), 노동OK→nodong.or.kr. kcomwel·산재요율은 현행 정답이라 무변경. stage-content.json codegen.
- **B3 운영URL** `c067d2e` — KIS POS kisinfo→kisvan, 오더플레이스(폐업)→페이히어, 아임포스→iposx, 샵캐스트→shopcast.kr, 와이즈비(미검증)→공비서, 두루누리→4insure. 웹+iOS 패리티, iOS BUILD SUCCEEDED.

---

## B. 외부 URL — 전수 매핑 (각 WebSearch 검증 후 수정 / 포털 전환 / 삭제)

### B1. 펀딩 프로그램 — `packages/shared/src/startup-programs.ts`
각 항목 **현재 URL**(아래)을 WebSearch로 살아있는지 확인 → 죽었으면 **주관기관 메인/사업목록**으로, 만료면 삭제.

| 프로그램 | 라인 | 현재 URL | 사장님 증상 | 조치 |
|---|---|---|---|---|
| D.CAMP 디데이 | 395 | `https://dcamp.kr/dday` | 페이지 사라짐 | dcamp.kr 현행 디데이/프로그램 목록 확인 |
| 모두의 창업 프로젝트 2026 | 728~741 | `https://www.k-startup.go.kr` | 일정 지남 | **삭제**(만료) — status "open"이나 시즌 "2-5월 신청" |
| 서울창업허브 | 471 | `https://seoulstartuphub.com` | 접속 불가 | 현행 도메인 확인(이전 가능성) |
| 희망리턴패키지-점포철거비 | 633 | `https://www.sbiz.or.kr/nhrp` | 페이지 없음 | 소진공 희망리턴패키지 현행(sbiz.or.kr 또는 semas) |
| 희망리턴패키지-재창업자금 | 652 | `https://www.sbiz.or.kr/nhrp` | 페이지 없음 | 〃 |
| 네이버 AI 해커톤 | 783 | `https://github.com/naver/ai-hackathon` | 2018 자료 | 현행 없으면 삭제 또는 네이버 커넥트재단 현행 |
| K-디지털 트레이닝 해커톤 | 813 | `http://www.k-digitalhackathon.kr/` | 페이지 없음 | HRD-Net 현행 또는 삭제 |
| 신한 SOL 인큐베이팅 | 939 | `https://www.shinhanfutureslab.com/` | 페이지 없음 | 신한 스퀘어브릿지/퓨처스랩 현행 |
| SK 트루이노베이션 | 954 | `https://truinnovation.sktelecom.com/` | 페이지 없음 | SK 트루이노베이션 현행 |
| 소프트뱅크 벤처스 아시아 | 1041 | `https://www.sbva.com/` | 페이지 없음 | 사명 변경 'SBVA' — sbva.com 확인(이름도 SBVA로?) |
| Antler Korea | 1111 | `https://www.antler.co/locations/seoul` | 페이지 없음 | antler.co 한국 현행 경로 |
| 서울여성 창업아이디어 공모전 | 1197 | `https://www.seoulwoman.or.kr/` | 페이지 없음 | 서울시 여성창업 현행 (deadline 2026-07-15 있음) |
| NH 디지털 Challenge+ | 1463 | `http://nhd-challengeplus.com/` | 접속 불가 | NH 현행 |
| 두루누리 사회보험료 지원 | 2072 | `https://insurancesupport.or.kr/` | 접속 불가 | **4insure.or.kr 두루누리 안내**로 통일(아래 B2와 동일 출처) |

- **만료 필터 헬퍼 없음** → 추가 권장:
  ```ts
  export function isProgramExpired(p: StartupProgram, now = new Date()): boolean {
    if (p.applicationStatus === "closed") return true;
    if (p.applicationDeadline) return new Date(p.applicationDeadline) < now;
    return false;
  }
  ```
  LoanGuideStage `matched`/live 렌더에서 `!isProgramExpired(p)` 필터.

### B2. 공식 사이트 — SSOT 콘텐츠 (⚠️ 수정 후 `gen-stage-content-json.mts` 재실행 → iOS 반영)
| 사이트 | 위치 | 현재 URL | 증상 | 조치 |
|---|---|---|---|---|
| 노동OK | `content/hiring-setup.ts:162` | `https://www.nodongok.com` | 경고창 | https 인증서/도메인 확인 |
| 홈택스 | `content/hiring-setup.ts:197` | `…/websquare.html?w2xPath=/ui/pp/index_pp.xml` | 접속 오류 | 메모리 `reference_hometax_url` PC 메인 URL 재확인. **간이세액표·반기납부 등 17단계 항목도 동일 URL 쓰는지 확인** |
| 근로복지공단 | `content/hiring-setup.ts:199` | `https://www.kcomwel.or.kr` | 경고 | 현행 인증서 확인 |
| 4insure(두루누리) | `content/hiring-setup.ts:196` | `https://www.4insure.or.kr` | (두루누리 접속불가와 연계) | 두루누리는 이 출처로 통일 |
| 산재보험 업종별 요율 | 17단계 데이터(위치 확인 필요) | — | 경고 | 근로복지공단/고용산재 토탈서비스 현행 |
| **홈택스 동일 URL 타 파일** | `permits/permit-matrix.ts:48`, `content/tax-guide.ts:260`, `content/registration-setup.ts:354·660` | 동일 | — | 홈택스 URL 변경 시 **이 5곳 일괄 교체** + 코드젠 |

### B3. 공급처·운영 URL
| 항목 | 위치 | 현재 URL | 증상/조치 |
|---|---|---|---|
| 헤이뷰티 | `vendor-setup-data.ts:235` | `https://heybeauty.me/` | **살아있음**(소비자 예약앱) — 미용실 입점용 POS로는 카테고리 애매. 유지 또는 삭제 판단 |
| 와이즈비/마이샵 | `operations-channels.json:69` | `https://search.naver.com/search.naver?query=와이즈비+예약관리` | ⚠️ **네이버 검색 URL** — 실제 회사 도메인으로 교체(WebSearch로 와이즈비/마이샵 공식 도메인 확인) |
| 토스플레이스 | `OperationsSetupStage.tsx:49` | `https://tossplace.com` | 사장님 "접속불가"라 했으나 정상 가능성 — 재확인 |
| **KIS정보통신** | POS `OperationsSetupStage.tsx:55` = `https://www.kisinfo.co.kr` / VAN `:167` = `https://www.kisvan.co.kr` | — | ⚠️ **두 URL 상이**. 사장님: "다른 단계선 정상, 18단계만 접속불가" → 18단계가 쓰는 게 어느 것인지 + 어느 도메인이 살아있는지 WebSearch로 확인해 통일 |
| 오더플레이스 | `OperationsSetupStage.tsx:61` | `https://www.orderplace.co.kr` | 접속불가 — 현행 확인 |
| 아임포스 | `OperationsSetupStage.tsx:73` | `https://www.ipos.co.kr` | 접속불가 — 현행 확인 |
| 샵캐스트 | `OperationsSetupStage.tsx:201` | `https://www.shopcast.co.kr` | 접속불가 — shopcast 현행 도메인 |

> 18단계 POS/VAN/음악 데이터는 `OperationsSetupStage.tsx` 47~218줄에 인라인. (operations-setup도 오프라인코어 SSOT 전환 대상(#5)이라, URL 교정과 SSOT 전환을 묶으면 효율적.)

---

## 검증 커맨드
```bash
cd "/Users/lij020218/New project"
npm run typecheck --prefix packages/shared          # SSOT 수정 후
cd apps/web && npx tsc --noEmit -p tsconfig.json    # "errs: 0"
cd "/Users/lij020218/New project" && npx tsx scripts/gen-stage-content-json.mts   # SSOT URL 수정 시 iOS 반영
# iOS: cd apps/ios && xcodebuild build -scheme FoundOneFeatures -destination 'generic/platform=iOS Simulator' -derivedDataPath build/ddp
```
커밋 시 `apps/ios/build/`·`artifacts/`는 이미 gitignore. 메시지 끝 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

## 권장 순서 (다음 세션)
1. **A2** AI 사업계획서 — 라우트 1개 파일 확인이 착수점(위 A2 §). 빠르게 끝날 가능성.
2. **B1 펀딩** — WebSearch 일괄 검증 → 죽은 건 주관기관 메인으로, 만료(모두의 창업 등) 삭제 + `isProgramExpired` 필터.
3. **B2 공식사이트** — 홈택스 5곳 + 노동OK·근로복지공단·두루누리·산재요율. 수정 후 코드젠.
4. **B3 운영 URL** — KIS 타이포 통일 + 와이즈비 도메인 + 오더플레이스·아임포스·샵캐스트. operations-setup SSOT(#5)와 묶기 검토.
