/**
 * Anti-hallucination 가드 — 모든 사장님-facing AI 응답이 거짓 숫자·정보를
 * 만들어내지 못하도록 시스템 프롬프트에 주입할 표준 블록.
 *
 *  ── 정책 (사용자 명령 2026-05-07) ──────────────────────────────
 *  1. AI 는 사장님 데이터에서 명시적으로 제공된 숫자만 인용한다.
 *  2. 외부 벤치마크·법령·통계 인용 시에는 prompt 의 [출처] 블록에 등장한
 *     이름·숫자만 사용 (Claude 자체 지식 기반 추측 금지).
 *  3. 검증 불가능한 항목은 *방향성* 만 서술 ("높은 편", "주목할 만") — 구체 수치 금지.
 *  4. 사례·브랜드·캠페인명은 prompt 에 인용된 경우에만 언급. 환각 시 즉시 카드 폐기.
 *  ──────────────────────────────────────────────────
 *
 *  사용:
 *  ```ts
 *  import { ANTI_HALLUCINATION_DIRECTIVE } from "../utils/anti-hallucination";
 *
 *  const systemPrompt = `당신은 ... 컨설턴트.
 *
 *  ${ANTI_HALLUCINATION_DIRECTIVE}
 *
 *  응답 형식: ...`;
 *  ```
 */

export const ANTI_HALLUCINATION_DIRECTIVE = `
🚨 절대 준수 — 거짓 숫자·정보 생성 금지:
1. **prompt 에 명시적으로 제공된 숫자만 인용**. 사장님 매출·비용·기간 등은 [사장님 현황] 블록 값만 사용.
2. **외부 통계·벤치마크는 prompt 의 [업종 벤치마크] [출처] 블록에 등장한 수치만 사용**. Claude 자체 지식 기반 통계 (예: "평균 마진 30%") 추론 금지.
3. **법령·세율·요율은 prompt 에 명시된 경우에만 인용**. 추측·기억 기반 인용 금지 (예: "최저시급 10,030" 같은 옛 수치 자동 회피).
4. **브랜드·사례·캠페인명은 prompt 에 등장한 경우에만 언급**. 일반 지식·기억 기반 사례 인용 금지.
5. **검증 불가능한 영역은 방향성 표현만** ("효과적", "주목할 만", "권장") — 구체 수치·비율은 prompt 근거 있을 때만.
6. **숫자 단위 정확히 인용**: "20만원" 은 200,000원, "200만원" 은 2,000,000원. 단위 혼동 금지.
`.trim();

/**
 * 사용자 데이터 그라운딩 헬퍼 — prompt 에 사장님 입력값을 삽입할 때 함께 붙이는 헤더.
 * AI 가 *이 블록 외부의 숫자를 만들어낼 때* 명백히 환각으로 분류 가능.
 */
export const GROUNDED_DATA_HEADER = `
[사장님 현황 — 검증된 서버 데이터]
※ 아래 수치는 사장님이 Found.One 에 직접 입력하신 값. 이 블록 외 숫자는 추측·환각이며 카드에 사용 금지.
`.trim();
