# build.up 서비스 기획 분석 보고서

**분석 일시:** 2026년 3월 26일
**분석 대상:** ~/Documents/New project (build.up 프로젝트)

---

## 1. 서비스 핵심 개요

build.up은 **한국 소자본 창업자를 위한 로드맵 기반 창업 동반자 앱**입니다.

창업 과정을 "업종 선택 → 창업 방식 결정 → 운영 모델 선택 → 예산 설정 → 입지 후보 비교 → 계약 검토 → 오픈 준비 → 허가/세금/대출 안내"라는 단계별 로드맵으로 구조화하고, 각 단계에서 의사결정을 도와주는 서비스입니다.

핵심 기능은 다음과 같습니다.

**워크플로 엔진:** 단계를 하나씩 완료해야 다음 단계가 열리는 결정론적 진행 구조. 선택지에 따라 분기(예: 프랜차이즈/독립 창업에 따라 다른 경로)하는 조건부 라우팅이 구현되어 있습니다.

**재무 시뮬레이션:** 자본금, 업종, 상권 특성을 입력하면 손익분기점, 월별 현금흐름, 생존 가능 기간, 리스크 등급을 자동 계산합니다. 업종별 벤치마크 데이터(매출, 원가율, 임대료 등)를 기반으로 합니다.

**AI 기반 분석 (5개 모듈):** 재무 해석, 가이드 Q&A, 상가 임대차 계약서 위험 분석, 상권 내러티브 생성, 단계별 브리핑. 모두 Anthropic Claude API를 사용하며, 구조화된 JSON 응답을 강제합니다.

**상권 분석:** 서울 25개 구 단위의 상권 데이터(임대료, 경쟁 밀도, 수요, 접근성 등)를 점수화하여 예산과 업종에 맞는 상권을 추천합니다.

**정보 신선도 관리:** 모든 사용자 대면 정보에 출처, 검증 일자, 다음 리뷰 일자를 메타데이터로 붙여 정보의 유효기간을 추적합니다.

**기술 스택:** Next.js(웹) + Expo(모바일) 모노레포, Supabase(인증/DB), TypeScript, pnpm + Turborepo.

---

## 2. 장점: 잘된 부분

**로드맵 퍼스트 설계 철학이 명확합니다.** "대시보드에 할 일을 쏟아붓지 않고, 한 번에 하나의 단계에만 집중한다"는 원칙이 코드 전반에 일관되게 반영되어 있습니다. 워크플로 엔진의 `evaluateStageCompletion`, `buildRoadmapState`, `completeCurrentStage` 함수들이 이 철학을 순수 함수로 깔끔하게 구현합니다. 창업 초보자의 인지 부하를 줄이는 좋은 접근입니다.

**"Logic before AI" 원칙이 실질적으로 지켜지고 있습니다.** 많은 스타트업이 AI를 과시하기 위해 모든 곳에 LLM을 넣지만, build.up은 워크플로 진행, 리스크 평가, 점수 계산 등 핵심 로직을 결정론적 코드로 처리하고, AI는 해석/설명/비교 등 자연어가 필요한 곳에만 사용합니다. 이는 제품의 예측 가능성과 신뢰성에 큰 장점입니다.

**재무 시뮬레이션 모델이 실용적입니다.** 매출 램프업 모델(1개월 30% → 5개월 100%), 생존 가능 기간 계산, 다층 리스크 스코어링 등이 소자본 창업자의 현실을 잘 반영합니다. 벤치마크 데이터가 없으면 합리적 기본값으로 폴백하는 설계도 좋습니다.

**AI 프롬프트 설계가 체계적입니다.** 모든 AI 모듈이 "규칙 → 응답 형식 → JSON 스키마"의 일관된 구조를 따릅니다. 특히 계약서 분석 프롬프트는 상가건물 임대차보호법 기반의 구체적 분석 기준을 제시하고, 가이드 Q&A는 confidence 수준(high/medium/check_needed)을 보수적으로 판단하도록 설계되어 있습니다.

**정보 신선도(Freshness) 개념이 인상적입니다.** 대부분의 창업 가이드 서비스가 한 번 만든 콘텐츠를 방치하는 반면, build.up은 모든 정보에 검증 일자, 다음 리뷰 일자, 출처 신뢰도를 메타데이터로 관리합니다. 아키텍처 문서에서도 "stale or blocked data should not silently appear"라고 명시하고 있습니다.

**모노레포 구조가 합리적입니다.** shared 패키지에 도메인 로직과 타입을 집중시키고, web/mobile이 이를 공유하는 구조입니다. AI 패키지도 별도로 분리되어 있어 관심사 분리가 잘 되어 있습니다.

**DB 설계에 RLS(Row Level Security)가 처음부터 적용되어 있습니다.** 모든 테이블에 사용자별 접근 정책이 설정되어 있어 보안 기본기가 탄탄합니다.

---

## 3. 단점: 부족하거나 약한 부분

**starter-stage-demo.tsx 파일이 11,345줄입니다.** 이것은 심각한 코드 품질 문제입니다. 하나의 React 컴포넌트 파일에 사실상 앱 전체의 UI 로직이 들어있습니다. 로드맵 화면, 재무 시뮬레이션 UI, 계약서 분석 UI, 상권 추천 UI, 프랜차이즈 정보, 가이드 Q&A 등 모든 기능이 하나의 파일에 몰려 있습니다. useDashboard.ts도 2,655줄로 과도합니다. 이 상태로는 유지보수, 코드 리뷰, 새 팀원 온보딩이 매우 어렵습니다.

**모바일 앱이 사실상 빈 껍데기입니다.** Expo 프로젝트가 존재하지만, 실질적인 기능 구현이 web에 집중되어 있습니다. 모바일 앱 파일 수가 12개에 불과하고, web의 복잡한 비즈니스 로직이 모바일에는 반영되지 않은 것으로 보입니다. 모노레포에서 shared를 공유한다는 설계 의도와 현실 사이에 괴리가 있습니다.

**테스트 코드가 전혀 없습니다.** 프로젝트 전체에서 테스트 파일(.test.ts, .spec.ts)이 발견되지 않습니다. 워크플로 엔진, 재무 시뮬레이션, 상권 점수 계산 등 순수 함수가 많아 단위 테스트에 매우 적합한 코드임에도 불구하고, 테스트가 없어 리팩터링이나 기능 추가 시 회귀 버그 위험이 큽니다.

**인증 시스템이 미완성입니다.** auth-notes.md에 "이메일 인증 비활성화", "UI 스크린 아직 미구현"이라고 명시되어 있습니다. 계정 생성 후 바로 접근 가능하다는 것은 MVP로는 이해하지만, 이메일 인증 없이 서비스를 운영하면 스팸 계정, API 남용 문제가 발생합니다.

**데이터가 대부분 하드코딩/시드 데이터입니다.** franchise-data.ts(937줄), starter-data.ts(1,899줄) 등 대량의 정적 데이터가 TypeScript 파일에 직접 들어있습니다. 아키텍처 문서에서 "future crawlers, API sync jobs"를 언급하지만, 현재 이를 자동으로 수집·갱신하는 파이프라인은 전혀 구현되어 있지 않습니다. Freshness 시스템의 설계 의도와 현실 사이에 큰 격차가 있습니다.

**Rate limiting이 인메모리 방식입니다.** API 라우트에 rate limit이 있지만, 서버 재시작 시 초기화되고 멀티 인스턴스 배포 시 작동하지 않습니다. 실 서비스에서는 Redis 기반 등으로 교체해야 합니다.

**에러 처리와 로딩 상태가 기본적입니다.** AI API 호출 실패, 네트워크 오류, Supabase 연결 실패 등에 대한 사용자 대면 에러 처리가 체계적이지 않습니다.

---

## 4. 개선점: 구체적 보완 사항

**즉시 실행해야 할 것:**

첫째, starter-stage-demo.tsx를 분해하세요. 기능 단위(RoadmapView, FinanceSimulator, ContractAnalyzer, MarketExplorer, GuideQA 등)로 컴포넌트를 분리하고, 공통 상태는 Context나 Zustand로 관리하는 것이 급선무입니다. 현재 구조로는 두 번째 개발자가 합류하는 순간 생산성이 급락합니다.

둘째, 핵심 순수 함수에 단위 테스트를 추가하세요. buildFinancialSimulation, evaluateStageCompletion, buildRecommendedMarkets 등은 외부 의존성 없이 입력-출력만 검증하면 되므로 테스트 작성이 쉽습니다. Vitest를 도입하면 좋겠습니다.

셋째, 정적 데이터를 DB로 이전하세요. franchise-data, starter-data, seoul-districts 등을 Supabase 테이블로 옮기고, 관리자 인터페이스나 최소한 스크립트를 통해 업데이트할 수 있게 만드세요. 현재처럼 코드에 박아두면 데이터 업데이트마다 배포가 필요합니다.

**중기적으로 개선할 것:**

넷째, 모바일 앱의 방향을 명확히 결정하세요. 웹과 동일한 기능을 모바일에서도 제공할 것인지, 아니면 모바일은 경량 알림/현황 확인 용도인지 결정하지 않으면 shared 패키지의 설계 범위가 흔들립니다. 현재 상태라면 모바일 앱을 제거하고 웹을 PWA로 제공하는 것이 리소스 효율적입니다.

다섯째, AI 응답 검증 레이어를 강화하세요. 현재 AI가 JSON을 반환하지 않거나 스키마에 맞지 않는 응답을 보내면 AiParseError로 처리하지만, 재시도 로직이 없고 사용자에게 유의미한 폴백도 없습니다. Zod 같은 런타임 스키마 검증과 1회 재시도를 추가하세요.

여섯째, 데이터 갱신 파이프라인을 만드세요. 상권 데이터, 프랜차이즈 정보, 정부 지원 프로그램 등은 주기적으로 변합니다. 최소한 크론 잡으로 공공데이터 API(소상공인시장진흥공단, 서울 열린데이터 등)에서 자동 수집하는 기초 인프라가 필요합니다.

---

## 5. 시장성/실현가능성에 대한 솔직한 의견

**시장 자체는 분명히 존재합니다.** 한국에서 매년 약 100만 건 이상의 신규 사업자 등록이 이루어지고, 그 중 상당수가 소자본 자영업입니다. 창업 과정의 복잡함(입지 선정, 인허가, 재무 계획, 계약 검토)은 실재하는 고통점이며, 이를 체계적으로 안내하는 서비스에 대한 수요는 있습니다.

**그러나 몇 가지 현실적 우려가 있습니다.**

첫째, 차별화의 깊이 문제입니다. 창업 가이드/로드맵 서비스는 소상공인시장진흥공단, 창업진흥원, 각종 프랜차이즈 플랫폼 등이 이미 무료로 제공합니다. build.up의 차별점은 "로드맵 기반 단계별 진행 + AI 해석"인데, 이것만으로 사용자가 기존 무료 서비스를 떠나 build.up을 선택할 만큼 강력한 동기가 되는지 검증이 필요합니다. 특히 계약서 분석이나 재무 시뮬레이션은 매력적이지만, 법적 면책 문제가 따라옵니다.

둘째, 데이터 경쟁력의 지속가능성입니다. 상권 데이터, 프랜차이즈 벤치마크, 임대료 시세 등은 서비스의 핵심 가치인데, 현재는 모두 초기 시드 데이터입니다. 이 데이터를 신선하고 정확하게 유지하는 비용은 서비스 규모와 무관하게 발생합니다. 1인 또는 소규모 팀이 이 데이터 품질을 지속적으로 유지할 수 있는지가 관건입니다.

셋째, 수익 모델이 불명확합니다. 코드와 기획 자료 어디에서도 수익 모델(구독, 프리미엄, B2B 등)에 대한 언급이 없습니다. AI API 비용(Anthropic Claude)은 사용량에 비례하여 발생하므로, 무료 서비스로 운영하기 어렵고, 유료화하려면 무료 대안 대비 확실한 가치를 증명해야 합니다.

넷째, 타겟 사용자의 디지털 리터러시입니다. 소자본 창업자 중 상당수는 40~60대이며, 복잡한 웹앱보다 카카오톡이나 전화 상담을 선호합니다. 이 서비스의 UX가 해당 타겟층에 맞는지 사용자 테스트가 필요합니다.

**실현가능성에 대해서는** 기술적으로 현재 코드베이스가 핵심 기능의 프로토타입으로서는 충분히 작동합니다. 워크플로 엔진과 재무 시뮬레이션 로직은 완성도가 높고, Supabase + Next.js 스택은 빠른 이터레이션에 적합합니다. 다만 프로덕션 수준으로 올리려면 앞서 언급한 코드 구조 개선, 테스트 추가, 데이터 파이프라인 구축이 선행되어야 합니다. 1인 개발자 기준으로 프로토타입에서 출시 가능한 MVP까지 최소 2~3개월의 추가 작업이 필요해 보입니다.

---

## 종합 평가

build.up은 **설계 철학과 핵심 도메인 로직에서 강한 기초**를 보여주는 프로젝트입니다. "로드맵 퍼스트", "Logic before AI", "정보 신선도 관리"라는 세 가지 원칙이 코드에 일관되게 반영되어 있고, 이는 많은 초기 스타트업이 놓치는 부분입니다.

반면, **코드 구조(만 줄짜리 단일 컴포넌트), 테스트 부재, 데이터 지속가능성, 수익 모델 부재**는 지금 당장 해결해야 할 과제입니다. 특히 starter-stage-demo.tsx의 분해는 더 미루면 미룰수록 비용이 기하급수적으로 증가합니다.

서비스의 가치 제안은 명확하지만, 시장에서 이기려면 "무료 공공 서비스보다 이게 왜 낫냐"에 대한 답이 필요합니다. AI 계약서 분석이나 맞춤형 재무 시뮬레이션이 그 답이 될 수 있지만, 법적 면책과 데이터 품질이라는 현실적 벽을 넘어야 합니다.
